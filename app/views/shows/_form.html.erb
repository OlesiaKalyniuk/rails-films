<%= form_with(model: show, local: true) do |form| %>
  <% if show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
        <% show.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :show_time %>
    <%= form.text_field :show_time %>
  </div>

  <div class="field">
    <%= form.label :film_id %>
    <%= form.text_field :film_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
